apiVersion: v1
kind: Service
metadata:
  name: student-portal
  namespace: default
spec:
  selector:
    app: student-portal
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
