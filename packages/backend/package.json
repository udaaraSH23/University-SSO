{
  "name": "@repo/backend",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -w",
    "test": "vitest"
  },
  "dependencies": {
    "@repo/auth": "*",
    "@repo/database": "*",
    "@repo/logger": "^0.0.0",
    "next": "16.1.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5"
  }
}
